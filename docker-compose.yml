version: '3.8'

services:
  vue-nginx:
    image: nginx:alpine
    container_name: vue-app
    ports:
      - "8080:80"
    volumes:
      - ./dist:/usr/share/nginx/html
      - ./nginx/conf.d:/etc/nginx/conf.d
    restart: unless-stopped